<%= form_with(model: @nekotapi, local: true) do |form| %>
  <% if nekotapi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nekotapi.errors.count, "error") %> prohibited this nekotapi from being saved:</h2>

      <ul>
      <% nekotapi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= @score %>
  <%= @nekotapi %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :memo %>
    <%= form.text_field :memo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
